@AGENTS.md

# 콘텐츠 제작 운영 관리 시스템 (목업)

## 이 프로젝트가 뭔지

(주)트립일레븐 AX 부서 채용 과제. 숙소·공간 콘텐츠 제작팀의 업무 흐름을 관리하는 **내부용 웹 목업**을 만든다.

과제 원문의 업무 흐름:

```
촬영 일정 관리 → 촬영 사진 업로드 → 사진 보정 및 검수 → 채널별 콘텐츠 업로드
```

**평가 포인트는 화면 완성도가 아니라 AX(AI Transformation) 관점의 문제 정의다.** 어디가 병목인지 짚어내고, 거기에 AI 또는 자동화를 정확한 위치에 붙였는지를 본다. 화면은 그 논리를 보여주는 수단이다.

## 절대 하지 말 것

- **백엔드를 만들지 마라.** 서버, DB, 로그인은 과제문에서 "필수 아님"으로 명시했다. 더미 데이터로 충분하다.
- **Python 워커, 실제 AI API 호출을 붙이지 마라.** 시간만 소모되고 화면에 보이는 결과는 동일하다.
- **인증, 회원가입, 권한 관리를 만들지 마라.**
- **기능을 늘리지 마라.** 아래 명세에 없는 화면을 추가하는 것보다, 명세에 있는 화면의 AX 표현을 정교하게 하는 게 점수가 높다.

## 기술 스택

- Next.js (App Router) + TypeScript
- Tailwind CSS
- 상태관리 라이브러리 없음. `useState` 로컬 상태로 충분
- 데이터는 `/data/*.ts` 파일에 상수 배열로 하드코딩
- 이미지는 `/public/photos/`에 더미 파일 배치 (없으면 회색 placeholder 박스로 대체)
- **Vercel 배포 필수.** 작업 중 수시로 `npm run build`를 돌려서 빌드가 깨지지 않는지 확인할 것. `next dev`는 통과하는데 `build`에서 타입 에러로 터지는 경우가 잦다.

---

# 1. 문제 정의 (설계의 근거)

화면을 만들기 전에 이 진단을 이해해야 한다. 모든 기능은 여기서 파생된다.

## 단계별 병목

| 단계 | 겉으로 보이는 일 | 실제 고통 |
|---|---|---|
| 촬영 일정 | 캘린더 관리 | 날씨·숙소 사정으로 밀린 일정 재조정 |
| 사진 업로드 | 파일 올리기 | 800~1000장 중 쓸 컷 셀렉. 그리고 **촬영 누락 발견이 너무 늦음** |
| 보정·검수 | 리터처 작업 | 반려 사유가 카톡에 흩어짐. 리터처별 톤 편차. 왕복 지연 |
| 채널 업로드 | 각 채널에 올리기 | 채널마다 규격·장수·문구 톤이 달라 **같은 일을 4~6번 반복** |

## 최악의 시나리오

편집 단계에서 "화장실 컷이 없네"를 발견하는 것. 이 시점엔 촬영팀이 철수했고, 숙소 재방문 협의를 다시 해야 하고, 며칠이 날아간다.

**이 시스템의 1차 목표는 그 발견 시점을 촬영 현장으로 당기는 것이다.**

## 설계 원칙 (모든 화면에 일관되게 적용)

> **AI/시스템이 초안 → 사람이 조정 → 확정**

AI는 사람의 판단을 대체하지 않는다. 판단해야 할 대상을 좁혀줄 뿐이다. 800장을 보던 사람이 3~4줄만 보게 만드는 것.

이 원칙은 UI에 반드시 드러나야 한다.
- AI 결과는 **단정형이 아니라 제안형**으로 표시
- 확신도(confidence)가 낮으면 "확인 필요" 표시
- 사람이 AI 판단을 뒤집는 버튼이 항상 존재 (예: 누락 경고 옆의 "이미 촬영함")

## AI를 쓰는 곳 / 안 쓰는 곳

이 구분 자체가 평가 포인트다. UI와 문서에 명시적으로 드러낼 것.

**"AI가 처리합니다"로 뭉뚱그리지 마라.** 수단이 세 층위로 나뉘고, 각각을 왜 골랐는지가 설계의 핵심이다.

### 1층: 순수 연산 (AI 아님)

학습 모델이 아니라 수식이다. 결과가 항상 같고, CPU 연산뿐이라 800장을 수 초에 처리한다. 여기에 LLM 비전을 쓰면 느리고 비싸고 오히려 부정확하다.

| 기능 | 수단 |
|---|---|
| 흔들림·초점 검출 | 라플라시안 분산 (엣지 선명도) |
| 노출 오류 | 히스토그램 분석 |
| 색온도 편차 | RGB 평균값 비교 |
| 수평 틀어짐 | 허프 변환 직선 검출 후 각도 계산 |

### 2층: 전용 모델

작고 빠르고 LLM 대비 훨씬 저렴하다. 로컬 실행도 가능.

| 기능 | 수단 |
|---|---|
| 중복·유사 컷 | 임베딩 벡터 코사인 유사도 (CLIP 계열) |
| 스마트 크롭 | 객체 검출 후 피사체 영역 계산 |
| 눈감음 검출 | 얼굴 랜드마크 |

### 3층: LLM 비전

**의미 이해가 필요한 것만.** 공간이 파우더룸인지 욕실인지는 수식으로 안 나오고 고정 클래스로도 부족하다. 숙소마다 공간 구성이 달라 유연한 판단이 필요하다.

| 기능 | 수단 |
|---|---|
| 공간 라벨링 | LLM 비전 |
| 촬영 누락 판단 | LLM 비전 + 체크리스트 대조 |
| 채널별 카피 생성 | LLM |

### AI를 아예 안 쓰는 것

| 기능 | 수단 |
|---|---|
| 담당자 배정·알림·리마인드 | 조건 분기 + 스케줄러 |
| 병목 대시보드 | 쿼리/집계 |
| 채널별 리사이즈 | 이미지 처리 라이브러리 |
| 필수 컷 대조 | 배열 차집합 |

### 처리 순서 = 비용 설계

```
사진 800장
   ↓
[1층] 기술적 결함 필터 — CPU 연산, 수 초
   흔들림 34, 노출 오류 8 제외 → 758장
   ↓
[2층] 중복 제거 — 임베딩
   유사 컷 412 제외 → 346장
   ↓
[3층] LLM 비전 라벨링 — 남은 것만
   중복 그룹은 대표 1장만 라벨링 후 나머지에 상속
   ↓
체크리스트 대조 (단순 배열 비교)
```

**LLM에 도달하기 전에 대상을 최대한 줄이는 게 핵심.** 800장을 전부 LLM에 던지면 비용과 시간이 몇 배가 된다. 앞단에서 싼 방법으로 걸러내고, 비싼 판단만 LLM에 넘긴다.

추가로 이미지는 원본이 아니라 **축소본(512~768px)을 전달한다.** 라벨링에 4000px이 필요 없고, 토큰 비용이 해상도에 비례한다.

---

# 2. 화면 명세

우선순위 순서다. 시간이 부족하면 뒤쪽을 버린다.

## 2-1. 대시보드 `/` — 우선순위 상

콘텐츠팀 리더가 아침에 여는 화면. "지금 뭐가 막혀 있나"를 3초 안에 파악.

**구성**

- 상단 KPI 4개
  - 진행 중 콘텐츠 건수
  - 정체 건수 (기준일 초과) — 빨간색 강조
  - 이번 주 촬영 예정
  - 이번 달 재촬영 발생 건수 ← **이 지표가 핵심.** 시스템의 존재 이유를 숫자로 보여줌
- 단계별 평균 소요일 바 차트
  - 촬영 1.2일 / 보정 **4.2일** / 검수 1.8일 / 발행 0.5일
  - 보정 구간이 시각적으로 튀게 만들 것. 병목이 어디인지 한눈에 보여야 함
- 정체 알림 리스트
  - "제주 ○○풀빌라 — 보정중 12일 경과 (담당: 김리터처)"
  - 클릭 시 해당 콘텐츠 상세로 이동

## 2-2. 파이프라인 보드 `/pipeline` — 우선순위 상

**이 프로젝트의 핵심 화면.** 4단계를 칼럼으로 세운 칸반 보드.

**칼럼**: 촬영 예정 / 촬영 완료 / 보정 중 / 검수 / 발행

**카드 표시 항목**
- 숙소명, 촬영일, 담당자
- 진행률 (예: 필수 컷 6/8)
- 정체 일수 배지 — 3일 초과 시 노랑, 7일 초과 시 빨강
- AI 경고 아이콘 — 누락 컷이 있거나 검수 플래그가 있으면

드래그앤드롭은 구현해도 되고 안 해도 된다. 시간 남을 때만.

## 2-3. 콘텐츠 상세 `/content/[id]` — 우선순위 최상

**평가 포인트가 전부 여기 몰려 있다. 가장 공들일 것.**

4개 탭으로 구성. 각 탭이 업무 흐름의 한 단계.

### 탭 1: 촬영

- 숙소 정보 요약 (숙소명, 유형, 촬영일, 작가)
- **필수 컷 체크리스트** — 이 화면의 주인공
  - 숙소 유형 템플릿에서 자동 생성됨. `자동 생성됨` 배지 표시
  - 각 항목: 라벨명 / 필요 장수 / 현재 장수 / 충족 여부
  - 예: `외관 3/2 ✓` `침실(디럭스) 12/5 ✓` `바비큐존 0/3 ✗`
  - 항목별 추가·제외 가능 (이 숙소만의 예외 처리)
- **현장 모드 토글**
  - 켜면 "프리뷰만 우선 업로드" 안내가 뜨고, 실시간으로 체크리스트가 채워지는 형태
  - 툴팁: "촬영 중 프리뷰(JPG)만 먼저 올려 누락을 현장에서 확인합니다. 원본은 복귀 후 업로드됩니다."
  - **이 토글이 AX 사고를 가장 잘 보여주는 장치다.** 같은 AI 기능이 사후 검수용이면 "재촬영을 빨리 아는 것"이고, 현장용이면 "재촬영이 아예 안 생기는 것"

### 탭 2: 업로드 / 분류

- 업로드 진행 UI
  - `247/800 완료` 진행률 바
  - 상태 배지: `프리뷰 업로드 완료 · 원본 대기중`
  - (실제 업로드는 구현하지 않음. 진행 중인 상태를 더미로 표현)
- **AI 누락 탐지 결과 패널** — 화면 상단 고정
  ```
  ⚠ 누락 의심 2건
  · 바비큐존 — 0장 (확신도 높음)          [이미 촬영함]
  · 수영장 야간 — 0장 (확신도 낮음, 확인 필요)  [이미 촬영함]
  ```
- **AI 라벨링 결과 그리드**
  - 사진 카드에 라벨 + 확신도 표시 (예: `욕실 · 94%`)
  - 확신도 80% 미만은 시각적으로 구분 (점선 테두리 등)
  - 라벨 수동 변경 가능
  - 라벨별 필터 탭
- **AI 셀렉 추천**
  - `800장 중 82장 추천` 표시
  - 제외 사유별 집계를 보여주되, **각 항목에 수단 설명 툴팁(ⓘ)을 붙일 것.** 3층 파이프라인이 화면에서 드러나야 한다

  ```
  AI 셀렉 · 800장 → 82장 추천
  ├ 흔들림 34장    ⓘ 라플라시안 분산으로 초점 선명도 측정 (연산)
  ├ 중복 412장     ⓘ 임베딩 유사도 비교 (전용 모델)
  ├ 노출 오류 8장   ⓘ 히스토그램 분석 (연산)
  └ 대표 컷 82장   ⓘ 공간 라벨링 후 라벨별 상위 선별 (LLM 비전)
  ```

  - "추천만 보기" 토글
  - 처리 단계별 소요 시간을 함께 표시하면 좋다: `결함 필터 3초 · 중복 제거 41초 · 라벨링 2분 18초`. 왜 이 순서인지가 숫자로 설명된다

### 탭 3: 보정 / 검수

흐름을 화면에 드러낼 것:

```
보정 의뢰 → 리터처 배정 → 다운로드 → [외부 작업: Lightroom/Photoshop] → 결과 업로드 → AI 1차 검수 → 사람 2차 검수 → 승인/반려
```

**중요**: 보정 작업 자체는 시스템 밖(라이트룸)에서 일어난다. 시스템은 배정·전달·검수·이력만 관리한다. 웹에서 보정하는 UI를 만들지 마라.

- 리터처 배정 영역 — 담당자, 마감일, 배정일시. 배정 시점이 기록돼야 정체 일수가 계산된다
- **원본 ↔ 보정본 비교 뷰** (좌우 분할 또는 슬라이더)
- **AI 1차 검수 플래그**
  - `수평 틀어짐 3장` `노출 편차 5장` `색온도 편차 2장`
  - 플래그된 사진만 필터링해서 보기
- **보정 톤 일관성 검사** — 같은 숙소 사진들의 색온도·밝기 분포를 시각화. 튀는 컷 표시
  - 리터처가 여러 명일 때 톤이 갈리는 실무 고질병에 대응
- 사진 단위 승인 / 반려 (일괄 아님)
- 반려 시 사유 필수 입력, 사진 위 핀 코멘트
- **반려 이력 표시** — 몇 차 반려인지, 이전 사유가 뭐였는지

### 탭 4: 발행

**AX 효과가 가장 극적으로 드러나는 구간.**

- 승인된 원본 세트 (예: 82장)에서 채널별로 자동 변환된 결과를 보여준다

| 채널 | 비율 | 장수 | 문구 톤 |
|---|---|---|---|
| 자사몰 | 4:3 | 20장 | 상세 설명형 |
| 네이버 | 1:1 | 10장 | 검색 키워드 중심 |
| 야놀자/여기어때 | 16:9 | 15장 | 프로모션 강조 |
| 인스타그램 | 4:5 | 10장 | 감성 짧은 문구 |

- 채널 탭 전환 시 해당 규격으로 크롭된 사진 세트가 표시
- **스마트 크롭 표시** — "피사체 인식 크롭 적용" 배지. 단순 중앙 크롭이 아님을 드러낼 것
- **LLM 생성 카피 초안**
  - 채널별로 톤이 다른 문구가 이미 채워져 있음
  - `AI 초안` 배지 + 편집 가능한 텍스트 영역
  - 인스타는 짧고 감성적으로, 네이버는 키워드 위주로 — 실제로 톤 차이가 나게 더미 텍스트를 써 넣을 것
- **채널별 발행 방식이 다르다는 걸 화면에 드러낼 것**

실제 API 정책을 조사한 결과다. 이 구분이 기획의 신뢰도를 만든다.

| 채널 | 자동 발행 | 방식 |
|---|---|---|
| 자사몰 | 가능 | 내부 API |
| 인스타그램 | 조건부 가능 | Graph API. 비즈니스/크리에이터 계정 + 페이스북 페이지 연결 + Meta 앱 심사 필요. 24시간당 100건 제한 |
| 야놀자·여기어때 | 협의 필요 | 공개 API 없음. 파트너 연동 또는 채널 매니저 경유 |
| 네이버 블로그 | **불가** | 글쓰기 API가 2020년 5월 종료됨 (광고성 대량 발행 문제) |

- 채널 카드에 상태 배지: `자동 발행` / `심사 필요` / `수동 발행`
- 자동 발행 채널: `발행 대기` → `발행 완료` 상태 전환 (실제 연동은 하지 않음)
- 수동 발행 채널: **"규격 맞춤 패키지 다운로드"** 버튼
  - 변환된 이미지 세트 + 카피 초안을 묶어서 내려받는 형태

**중요한 메시지**: 자동 발행이 안 되는 채널이 있어도 이 시스템의 가치는 대부분 유지된다. 진짜 반복 노동은 "업로드 버튼 누르기"가 아니라 **채널마다 사진을 자르고 고르고 문구를 쓰는 것**이고, 그 부분은 API 없이도 전부 자동화된다. 화면 어딘가에 이 취지가 한 줄로 드러나면 좋다.

## 2-4. 캘린더 `/calendar` — 우선순위 중

월간 뷰. 날짜별로 촬영 일정 카드 배치. 클릭 시 콘텐츠 상세로 이동.

캘린더는 **미래**를 보는 화면이고, 파이프라인 보드는 **현재**를 보는 화면이다. 역할이 다르니 둘 다 있어야 한다.

## 2-5. 숙소 관리 `/accommodations` — 우선순위 중

- 리스트 페이지
- 등록/수정 폼
  - 숙소명, 유형, 주소
  - 시설 정보 체크박스: 객실 타입 수, 수영장, 조식, 바비큐, 반려동물 등
- **핵심**: 시설 정보를 입력하면 필수 컷 목록이 자동 생성되는 것을 보여줄 것

```
객실 3타입 / 야외수영장 / 조식 O / 바비큐 O
   ↓ 자동 생성
외관, 로비, 디럭스룸, 스위트룸, 스탠다드룸,
욕실 3종, 수영장, 조식장, 바비큐존
```

**이게 "AI 안 쓰는 자동화"의 좋은 예시다.** 이미 가진 데이터를 연결해서 입력 자체를 없앤 것. 화면에 이 인과관계가 보이게 만들 것.

## 2-6. 처리 구조 설명 `/how-it-works` — 우선순위 중상

**기획서의 웹 버전.** PDF를 안 여는 면접관도 사이트에서는 보게 되므로 전달률이 높다. 정적 페이지 하나면 되고, 만드는 시간 대비 효과가 크다.

담을 내용:

1. **As-Is 흐름과 병목** — 4단계별로 지금 뭐가 아픈지
2. **3층 처리 파이프라인** — 800장이 각 단계에서 어떻게 줄어드는지 다이어그램으로. 왜 이 순서인지(비용·속도) 명시
3. **수단 선정 근거표** — 연산 / 전용 모델 / LLM / AI 아님 구분
4. **저장 계층 분리** — RAW / 프리뷰 / 썸네일. 용량 계산 포함
5. **채널별 발행 가능 여부** — 위 표
6. **기대 효과 4층위** — 시간 단축 / 낭비 제거 / 품질 균일화 / 가시성 확보

읽는 문서라서 다른 화면과 톤이 달라도 된다. 다만 별도 사이트처럼 보이면 안 되고, 같은 레이아웃 안에 있어야 한다.

## 2-7. 유형 템플릿 관리 — 만들지 말 것

기획서에 언급만 한다. 시간 대비 효과가 낮다.

---

# 2-8. 목업 범위 표시 (전 화면 공통)

**목업이라는 걸 감추지 마라.** 명시하는 게 오히려 점수가 된다. "이거 실제로 돌아가나요?"라는 질문에 어색해지는 것보다 낫다.

- 사이드바 하단 또는 상단에 고정 안내

```
이 화면은 더미 데이터 기반 목업입니다.
AI 처리 결과는 실제 파이프라인의 출력 형태를 재현한 것입니다.
```

- AI가 생성한 영역에는 `AI 결과 (모의)` 배지. 어디까지가 실제 로직이고 어디부터가 가정인지 명확히
- 카피 초안에는 `AI 초안` 배지

**범위를 정직하게 표시하는 것 자체가 실무자의 태도로 읽힌다.**

---

# 3. 더미 데이터 설계

`/data` 폴더에 아래 구조로 작성. 실제 DB 스키마와 형태를 맞춰서, 나중에 백엔드를 붙이면 그대로 대응되게 한다.

```ts
// 숙소
{ id, name, type, address, facilities: { roomTypes: 3, pool: true, breakfast: true, bbq: true } }

// 필수 컷 정의 (유형별 템플릿에서 생성)
{ id, accommodationId, label, minCount, isRequired, autoGenerated: true }

// 콘텐츠 (촬영 건)
{ id, accommodationId, shootDate, photographer, retoucher,
  status: '촬영예정'|'촬영완료'|'보정중'|'검수'|'발행',
  statusChangedAt, stuckDays, reshootCount }

// 사진
{ id, contentId, previewUrl, thumbUrl, rawPath,
  aiLabel: '욕실', confidence: 0.94,
  selected: true, excludeReason: null,
  reviewFlags: ['수평 틀어짐'],
  approvalStatus: '승인'|'반려'|'대기', rejectHistory: [] }

// 채널 프로필
{ id, name: '네이버', ratio: '1:1', maxPhotos: 10, tone: '검색 키워드 중심' }
```

**데이터는 그럴듯해야 한다.** 숙소명은 실제 있을 법하게(제주, 강릉, 가평 등), 정체 일수는 편차 있게, 확신도는 0.6~0.99 범위로 흩어지게. 모든 사진의 확신도가 0.95면 AI를 안 써본 티가 난다.

이미지는 `/public/photos/`에 20장 내외. 없으면 라벨명이 적힌 회색 박스로 대체해도 무방하다.

---

# 4. 디자인 방향

**레퍼런스: Notion 데이터베이스 뷰.** 업무용 내부 도구다. 마케팅 랜딩페이지가 아니다.

"예쁘게"보다 **"일하는 화면처럼"**이 목표다. 내부 도구가 화려하면 실무를 안 써본 티가 난다.

## 핵심 원칙

- **색은 상태 전달에만.** 장식용 색을 쓰지 않는다
- **정보 밀도 우선.** 여백을 넉넉히 두지 마라. 한 화면에서 많은 걸 파악할 수 있어야 한다
- **테두리로 구조를 만든다.** 그림자가 아니라 1px 선으로. 노션이 그렇다
- **AI 산출물은 화면 전체에서 같은 색으로 표시.** 어디가 AI가 만든 건지 항상 구분되게

## 색 토큰

이 값만 쓴다. 다른 색을 추가하지 마라.

```
텍스트
--text-primary:   #37352F   (노션 기본 텍스트)
--text-secondary: #787774
--text-tertiary:  #9B9A97

배경/선
--bg:             #FFFFFF
--bg-subtle:      #F7F6F3   (헤더, 호버, 테이블 줄무늬)
--border:         #E9E9E7
--border-strong:  #DFDFDE

상태
--warning:        #D9730D   (3일 초과 정체)
--danger:         #E03E3E   (7일 초과 정체, 누락)
--success:        #0F7B6C   (승인, 충족)

AI 관련 (단일 색으로 통일)
--ai:             #6940A5   (보라. AI 산출물·제안·배지 전용)
--ai-bg:          #F4F0F8
```

배지는 연한 배경 + 진한 텍스트 조합. 노션 태그와 같은 방식.

## 타이포

- 폰트: `Pretendard`(한글) 우선, 없으면 시스템 산세리프. 웹폰트 하나만 쓴다
- 크기 5단계로 제한: `11px` (배지·캡션) / `13px` (본문·테이블) / `14px` (기본 UI) / `16px` (섹션 제목) / `22px` (페이지 제목)
- **본문 기본이 13px다.** 업무 도구는 작다. 16px로 키우지 마라
- 굵기는 400 / 500 / 600만 사용
- 대문자 라벨(`ALL CAPS`) 쓰지 마라

## 간격·형태

- 기준 단위 4px. 여백은 4의 배수로만
- 테이블 행 높이 `36px`, 셀 좌우 패딩 `10px`
- 카드 패딩 `16px`. 그 이상 주지 마라
- `border-radius`는 `4px` 하나만. 요소마다 다른 값 쓰지 마라
- **그림자 사용 금지.** 드롭다운·모달 같은 떠 있는 요소에만 최소한으로

## 하지 말 것

AI 생성 페이지의 전형적 특징이다. 하나라도 나오면 수정할 것.

- 모든 콘텐츠를 동일한 라운드 카드로 자르기
- 그라데이션 배경, 히어로 섹션
- 전 섹션 페이드인/슬라이드업 애니메이션
- 카드마다 호버 시 뜨는 효과
- 버튼·링크 텍스트에 화살표(→) 붙이기
- 제목 위에 대문자 eyebrow 라벨 달기
- 메타 정보를 가운뎃점(`A · B · C`)으로 잇기

모션은 **사용자 동작에 대한 반응**에만 쓴다. 탭 전환, 아코디언 열림, 토스트 정도.

## 작업 방식

**화면 하나를 먼저 완성하고 나머지를 거기 맞춰라.** 전체를 동시에 만들면 화면마다 톤이 갈린다.

순서: 콘텐츠 상세 탭 1개를 기준 품질로 완성 → 공통 컴포넌트(배지, 테이블, 카드, 탭) 추출 → 나머지 화면은 그 컴포넌트로만 조립.

## 반응형

데스크톱 우선. 단 **현장 모드 관련 화면(촬영 탭, 업로드 탭)은 모바일에서 동작**해야 한다. 촬영 현장에서 폰으로 쓰는 시나리오라 이 부분은 실제로 확인할 것.

---

# 5. 실제 도입 시 아키텍처 (문서용, 구현 안 함)

기획서에 들어갈 내용. 코드로 만들지 말고, 필요하면 시스템 안에 별도 페이지(`/architecture`)로 정적 표시만 해도 좋다.

## 저장 계층 분리

촬영 원본 RAW는 장당 25~50MB. 한 건에 800장이면 20~40GB, 월 30건이면 **월 600GB~1.2TB**. 일반 웹서버 디스크로는 불가능한 규모다.

| 대상 | 저장 위치 | 용도 |
|---|---|---|
| RAW 원본 | 콜드 스토리지 (S3 Glacier / NAS) | 보관. 거의 안 꺼냄 |
| 프리뷰 (긴 변 2048px) | 오브젝트 스토리지 | 검수 화면 |
| 썸네일 (400px) | CDN | 목록 |

**웹 시스템은 프리뷰와 썸네일만 다룬다.** DB에는 경로와 메타데이터만 저장.

## 업로드 전략

브라우저에서 서버로 800장을 보내는 구조는 실패한다 (타임아웃, 대역폭, 중간 실패 시 전체 롤백).

```
브라우저 → 서버: presigned URL 요청
서버 → 브라우저: URL 발급 + DB에 pending 레코드 생성
브라우저 → 스토리지: 직접 업로드 (동시 4~6개, 서버 경유 안 함)
스토리지 → 서버: 완료 이벤트
서버: 상태 갱신 → AI 처리 큐 투입
```

파일이 웹서버를 거치지 않는 게 핵심. 재개 가능 업로드(파일 해시로 중복 판정)도 포함.

## 현장 프리뷰 우선 업로드

24GB를 올리는 데는 물리적으로 30분 이상 걸린다. 어떤 기술로도 못 줄인다.

해법: 카메라의 RAW+JPG 동시 저장을 활용해, **현장에서는 JPG(장당 5MB, 총 4GB)만 먼저 업로드.** LTE로도 가능하고, AI 라벨링과 누락 탐지는 이 프리뷰로 충분히 돌아간다. RAW는 복귀 후 유선으로 야간 업로드.

**이게 "현장 모드"를 기술적으로 성립시키는 근거다.**

## 기술 스택 선택 근거

목업: Next.js 단일 프로젝트 + Vercel 배포.

실제 도입 시:
- 기존 시스템이 있다면 **갈아엎지 않는다.** AI 처리 계층만 분리해서 HTTP로 연결
- 이미지 처리(EXIF 파싱, 리사이즈, 유사도 기반 중복 제거, 색온도 편차 검출)는 Python 워커로 분리. 큐로 연결해 대량 배치가 웹 응답을 막지 않게
- DB 직접 노출 대신 API 계층을 두는 쪽이 안전 (서버리스는 요청마다 커넥션이 새로 열려 커넥션 고갈 위험)

새 언어·새 스택을 쓸 줄 아는 것보다, **안 쓸 때를 판단하는 게 도입 담당자의 역할이다.**

---

# 6. 기대 효과 (기획서용)

시간 단축만 말하면 다른 지원자와 같아진다. 네 층위로 정리한다.

| 층위 | 내용 |
|---|---|
| 시간 단축 | 라벨 분류·1차 보정·채널 변환 자동화 |
| **낭비 제거** | 촬영 누락을 현장에서 잡아 재촬영 자체를 없앰 |
| **품질 균일화** | 리터처가 달라도 톤 편차가 관리됨 |
| **가시성 확보** | 병목 단계가 숫자로 드러남 |

시간 단축은 누구나 쓴다. **재촬영을 없애는 것**과 **병목이 보이게 하는 것**은 프로세스를 본 사람만 쓸 수 있다.

## 확장 아이디어 (문서에 한 줄씩만)

- 반려 사유가 누적되면 패턴이 보인다. LLM으로 요약해 리터처별 가이드를 만들면 반려 자체가 줄어든다
- 촬영 누락 현장 감지와 마찬가지로, **사후 처리에서 사전 예방으로 넘어가는 사례**

---

# 7. 작업 순서

1. `create-next-app` + Tailwind 세팅, **GitHub 푸시, Vercel 연결까지 먼저 끝낼 것** (빈 페이지 상태로)
2. 더미 데이터 작성 (`/data`)
3. 공통 레이아웃 + 사이드바 네비게이션
4. 콘텐츠 상세 4개 탭 ← **여기에 시간의 절반을 쓴다**
5. 파이프라인 보드
6. 대시보드
7. `/how-it-works` 페이지
8. 캘린더, 숙소 관리
9. `npm run build` 확인 후 최종 배포

각 단계마다 커밋하고 푸시할 것. 언제든 배포된 상태가 유지되게.

---

# 8. 제출물

| 항목 | 형태 |
|---|---|
| 목업 사이트 | Vercel 링크 |
| 기획서 | PDF (As-Is 진단 → 병목 → AI 적용 지점 → 아키텍처 → 기대 효과 + 주요 화면 스크린샷) |
| 소스코드 | GitHub 링크 |

면접관이 개발자가 아닐 수 있다. **링크 하나로 열리게 하는 것이 요구사항의 일부다.** 기획서 PDF에 스크린샷을 넣어 링크를 못 여는 상황까지 대비한다.
